1. HEAD 请求与 GET 请求类似, 但返回值中没有响应主体(body), 但有返回代码

2. HEAD 请求可以获取相关资源的头部信息, 在支持缓存的系统中, 这些头部信息可以用于检测获得的信息是否有效, 或者检测出这些信息最近是否有被修改过

3. HEAD 请求可以用于探测资源是否存在, 如果存在则返回 200, 如果不存在则返回 404

4. HEAD 请求并不能提高 API 的性能, 只是返回头部信息

5. 使用时只需要加入 [HttpHead] 即可生效, 可与其他特性连用